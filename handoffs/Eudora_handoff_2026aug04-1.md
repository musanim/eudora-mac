Project folder: /Users/stephenmalinowski/ClaudeProjects/Eudora

# Handoff: Eudora — 2026aug04

**Supersedes `Eudora_handoff_2026aug02.md`.** Everything that handoff listed as
untested (the Find-window preview pane, the centred "View in Mailbox") has since
been built and used, and its "Next steps" are all done. Nothing in it still
applies; it can be moved to `handoffs/archive/`.

---

## ▶ START HERE

Follow `/Users/stephenmalinowski/ClaudeProjects/GenericClaudeSessionStartup.md`,
which in short means:

1. Mount `/Users/stephenmalinowski/ClaudeProjects/Eudora` (pass the path
   directly; don't ask).
2. Mount `/Users/stephenmalinowski/ClaudeProjects` and read
   `claude-conventions.md` there.
3. Read this project's `CLAUDE.md` — short, and every line was paid for.
4. Read this handoff. Then **stop and report** — say what you mounted and read,
   summarize where things stand, and wait. Don't start on "Next steps" without
   Stephen confirming.

Two rules worth having in front of you before you type anything:
**one step at a time** (give one command, wait for the result), and
**nothing actionable after a command block** — Stephen reads and acts in order,
so a caveat placed after the commands arrives too late.

---

## Goal

Eudora 8: a native macOS replacement for Eudora 7. The cutover is long done;
Stephen's real 12 GB tree is live and both POP accounts collect into it. Work is
now ordinary improvement, driven by him using the app all day.

## Completed this session

All built, run and confirmed by Stephen unless said otherwise.

- **Tests for `MailStore.message(at:offset:)`** — `MailStoreOffsetReadTests`,
  12 cases, green. The one worth knowing is a glued record boundary whose
  envelope date runs past the 64K seam; it pins down the `+ envelopeDateLength`
  half of the reader's overlap, which the other seam test does not.
- **Three tests that had never been run**, found by running the whole suite
  rather than the new file. Two were real bugs: `EmailAddress.scan` left an HTML
  entity on the domain (silently disabling the filing-suggestion body fallback),
  and `LinkSafety.isMixedScript` was unreachable because `URL(string:)`
  IDNA-encodes before `assess` sees the host. Suite is 472, 0 failures.
- **Developer ID signing and notarization.** `scripts/release.sh` builds, signs,
  verifies, notarizes, staples and packages. `Build Release to Share.command` at
  the repo root does the whole thing from a double-click, including the version
  bump. Last run shipped `dist/Eudora-0.3.3.zip`; 0.3.2 went to Julius Smith.
- **Green new-mail ball** in the mailbox tree (`TreeIcon.newMail`, 20 pt),
  generated by `assets/make-tree-newmail.py`. Message rows keep the original.
- **Blacklist changes**: moved to the bottom of the message context menu behind a
  deliberate gap, and it now deletes the message as well.
- **Context menu centred on the pointer** instead of anchored to the row's left
  edge, which on a 13-inch-wide row threw it a foot from his hand.
- **⇧-arrow multi-select no longer skips rows**, and a multi-selection now
  **previews the message just added** rather than showing a count.

## Current state

Everything above is committed and pushed. The working tree should be clean apart
from anything Stephen has done since.

`AppModel.diagnoseSelection` is **off but intact**, next to the other dormant
diagnostics. It earned its place: it settled the ⇧-arrow preview bug in one run,
after reasoning had produced a fix that looked right and was wrong.

## Next steps

Nothing is queued. Stephen drives. Two things are recorded as open, neither
urgent:

1. **A message read inside a multi-selection stays marked unread.**
   `scheduleMarkSelectedRead` is still gated on a single selection. Deliberate,
   not an oversight — ⇧-arrowing through fifty messages to pick three would
   otherwise mark all fifty read — but the comment that justified it is now only
   half true, and it says so. One line to change if Stephen decides otherwise.
2. **⇧-arrow is correct but halting in a huge mailbox.** Main-actor congestion
   from enrichment re-diffing the table between keystrokes. Stephen explicitly
   declined to chase it. If it is ever worth attacking, `sample $(pgrep -x
   Eudora) 10 -file out.txt` — not homemade counters, and the binary is `Eudora`,
   not `EudoraApp`.

## Key context

**`EudoraDevelopmentNotes.txt` is the running record and it is long.** Every
feature has a section with a "To test" list. It is gitignored. Keep appending in
the same style. A datestamp note at the end explains that this session ran past
midnight Pacific, so some entries stamped 2026aug03 were written on the 4th.

**Use `general-purpose` review agents before handing anything over.** They earned
their keep repeatedly today and caught things that would have shipped broken: a
`grep -q` + `pipefail` interaction that made a release check fail precisely when
it should pass; a veil teardown that would have blanked a 22,000-row Trash and
lost the scroll position; a selection rule that handled growth and left every
other gesture arbitrary; a "Forward" menu item that would have silently done
nothing.

**Instrument rather than theorise — this is not a slogan here.** The ⇧-arrow
preview bug looked like a state-timing problem, was reasoned about at length, and
was actually a wrong assumption underneath correct-looking code: distance between
selected rows was being measured in *message ID*, but the list is sorted by date,
so IDs are neither consecutive nor monotonic on screen. No amount of reading
would have shown it; one trace did immediately.

**Releasing** is `Build Release to Share.command` — double-click. Signing lives in
`project.yml` and its comments are load-bearing: the Developer ID fingerprint, the
deliberately empty `DEVELOPMENT_TEAM`, and two **Release-only** settings that
exist because Apple's notary service rejected the build without them. Don't move
those into `base`. Three separate Apple certificate types are easy to confuse; the
notes have the full account.

**Claude must not run git.** The sandbox can't unlink files in `.git`, so every
git command strands a lock file that blocks Stephen's own git. Write the commit
message to `reference/commit-message.txt` (gitignored) and give him
`git commit -F`. Recovery from a stranded lock: `rm -f .git/index.lock`.

**Claude cannot build.** No Swift toolchain. The loop is write carefully →
Stephen builds → he pastes errors. Library code in `EudoraMac/` *can* be verified:
`cd EudoraMac && swift test`. Prefer putting logic there. Note `swift test` now
prints a cheerful "Test run with 0 tests in 0 suites passed" from the
swift-testing harness after the XCTest summary — that checkmark is not the
result; the line that matters is "Executed N tests, with M failures".

**Assets:** a new imageset inside `Assets.xcassets` needs no `xcodegen`; a new
file under `EudoraApp/Sources` does, and run it *after* writing the file.

**Stephen's PATH shadows common tool names** — Humdrum ships its own `ditto`
ahead of `/usr/bin`. Scripts here call Apple tools by absolute path for that
reason. Assume any short tool name may be shadowed on this machine.

The user will likely ask you to work on whatever he has run into during the day's
use — there is no queued backlog.
