# Generated by XcodeGen from project.yml — do not commit.
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
.swiftpm/
.build/
Package.resolved

# macOS
.DS_Store

# Local secrets — never commit
EudoraTestPassword.txt

# Vendored third-party reference source (kept locally, not tracked)
reference/

# Real mail — private test data, never commit (repo is public)
phaseX/
Eudora_*.zip
